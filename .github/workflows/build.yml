name: Muledump.NET Build & Release

on:
  push:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout Muledump.NET repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Checkout RotMGAssetExtractor (temp)
        uses: actions/checkout@v4
        with:
          repository: TadusPro/RotMGAssetExtractor
          path: RotMGAssetExtractor_temp

      - name: Move extractor up one level so path matches '..\\RotMGAssetExtractor'
        shell: pwsh
        run: Move-Item -Force RotMGAssetExtractor_temp ..\RotMGAssetExtractor

      - name: Setup .NET 8
        uses: actions/setup-dotNET@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore deps
        run: dotNET restore

      - name: Publish Muledump.NET (same command you run locally)
        run: |
          dotNET publish -c Release -f net8.0-windows10.0.19041.0 -r win-x64 `
            -p:WindowsPackageType=None -p:PublishReadyToRun=false -o publish

      - name: Download WebView2 runtime (Evergreen bootstrapper)
        run: curl -L "https://go.microsoft.com/fwlink/p/?LinkId=2124703" -o publish\MicrosoftEdgeWebview2Setup.exe

      - name: Zip output (portable)
        shell: pwsh
        run: Compress-Archive -Path "publish/*" -DestinationPath "Muledump.NET-portable-windows-x64.zip"

      - name: Create SHA256 for installer
        shell: pwsh
        run: |
          $f = "installer/Muledump.NET-setup-windows-x64.exe"
          $hash = (Get-FileHash -Algorithm SHA256 $f).Hash
          $hash | Out-File "$f.sha256" -Encoding ascii
          echo "INSTALLER_HASH=$hash" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Install & build installer (Inno Setup)
        shell: pwsh
        run: |
          choco install innosetup -y
          if ("${{ github.ref }}".StartsWith("refs/tags/v")) {
            $v = "${{ github.ref_name }}".TrimStart('v')
          } else {
            $v = "0.0.${{ github.run_number }}"
          }
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" .github\workflows\Muledump.iss /DMyAppVersion=$v /DInstallerHash="${{ env.INSTALLER_HASH }}"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Muledump.NET-build
          path: |
            Muledump.NET-portable-windows-x64.zip
            installer/Muledump.NET-setup-windows-x64.exe
            installer/Muledump.NET-setup-windows-x64.exe.sha256

      - name: Upload assets to the "latest" release (always)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest
          files: |
            Muledump.NET-portable-windows-x64.zip
            installer/Muledump.NET-setup-windows-x64.exe
            installer/Muledump.NET-setup-windows-x64.exe.sha256
          make_latest: true
          token: ${{ secrets.GITHUB_TOKEN }}
          skip_if_release_exists: false
